apiVersion: security.istio.io/v1
kind: PeerAuthentication
metadata:
  name: strict-mtls
  namespace: production
  uid: pa-strict-001
spec:
  selector:
    matchLabels:
      app: api
  mtls:
    mode: STRICT
